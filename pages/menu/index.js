import styled from "styled-components";

const Stack = styled.div`
  font-size: 1.5em;
  text-align: center;
  color: white;
  position: relative;
`;

const Menu = () => {
  return <Stack></Stack>;
};

export default Menu;
